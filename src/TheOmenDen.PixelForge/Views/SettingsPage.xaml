<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TheOmenDen.PixelForge.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="4">
            <StackPanel Margin="0,0,0,16" Spacing="4">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Settings" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Choose how the app looks, and tell it where your art packs live." />
            </StackPanel>

            <TextBlock
                Margin="0,0,0,8"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="Appearance" />

            <controls:SettingsCard Description="Applies immediately." Header="App theme">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE771;" />
                </controls:SettingsCard.HeaderIcon>
                <!--  Three short mutually-exclusive toggles — the same case as the export mode.  -->
                <controls:Segmented
                    AutomationProperties.AutomationId="ThemeSelector"
                    AutomationProperties.Name="App theme"
                    SelectedIndex="{x:Bind ViewModel.SelectedThemeIndex, Mode=TwoWay}"
                    SelectionMode="Single">
                    <controls:SegmentedItem Content="System" ToolTipService.ToolTip="Follows whatever light or dark setting Windows is using." />
                    <controls:SegmentedItem Content="Light" ToolTipService.ToolTip="Always use the light look, whatever Windows is set to." />
                    <controls:SegmentedItem Content="Dark" ToolTipService.ToolTip="Always use the dark look, whatever Windows is set to." />
                </controls:Segmented>
            </controls:SettingsCard>

            <TextBlock
                Margin="0,24,0,8"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="Source packs" />

            <InfoBar
                x:Name="PacksHintBar"
                Margin="0,0,0,8"
                AutomationProperties.AutomationId="PacksHintBar"
                IsClosable="False"
                IsOpen="True"
                Message="The Time Elements art packs are not included with Pixel Forge, because the license does not let us pass them on. Get the three packs yourself, then point each row below at that pack's assets folder. The Assets and Pipeline pages stay empty until all three are set, and so does the preview on the Palette page."
                Severity="Informational" />

            <controls:SettingsCard
                AutomationProperties.AutomationId="CorePackPath"
                Description="{x:Bind ViewModel.CorePackPath, Mode=OneWay}"
                Header="Core pack">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE8B7;" />
                </controls:SettingsCard.HeaderIcon>
                <Button
                    AutomationProperties.AutomationId="BtnBrowseCorePack"
                    AutomationProperties.Name="Browse for core pack folder"
                    Command="{x:Bind ViewModel.BrowseCorePackCommand}"
                    Content="Browse"
                    ToolTipService.ToolTip="Pick the assets folder inside the main Time Elements pack. It should hold folders named head, top, bottom, hair, and so on. The path is saved right away." />
            </controls:SettingsCard>

            <controls:SettingsCard
                AutomationProperties.AutomationId="Expansion1PackPath"
                Description="{x:Bind ViewModel.Expansion1PackPath, Mode=OneWay}"
                Header="Character Expansion 1">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE8B7;" />
                </controls:SettingsCard.HeaderIcon>
                <Button
                    AutomationProperties.AutomationId="BtnBrowseExpansion1Pack"
                    AutomationProperties.Name="Browse for expansion 1 folder"
                    Command="{x:Bind ViewModel.BrowseExpansion1PackCommand}"
                    Content="Browse"
                    ToolTipService.ToolTip="Pick the assets folder inside Character Expansion 1. It is a separate download from the main pack and has its own extra hair and clothes." />
            </controls:SettingsCard>

            <controls:SettingsCard
                AutomationProperties.AutomationId="Expansion2PackPath"
                Description="{x:Bind ViewModel.Expansion2PackPath, Mode=OneWay}"
                Header="Character Expansion 2">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xE8B7;" />
                </controls:SettingsCard.HeaderIcon>
                <Button
                    AutomationProperties.AutomationId="BtnBrowseExpansion2Pack"
                    AutomationProperties.Name="Browse for expansion 2 folder"
                    Command="{x:Bind ViewModel.BrowseExpansion2PackCommand}"
                    Content="Browse"
                    ToolTipService.ToolTip="Pick the assets folder inside Character Expansion 2. This one has no front extra folder, so that section will not appear on the Pipeline page." />
            </controls:SettingsCard>
        </StackPanel>
    </ScrollViewer>
</Page>
