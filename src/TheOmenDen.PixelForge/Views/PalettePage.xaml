<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TheOmenDen.PixelForge.Views.PalettePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:local="using:TheOmenDen.PixelForge.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:palettes="using:TheOmenDen.PixelForge.Core.Palettes"
    xmlns:vm="using:TheOmenDen.PixelForge.ViewModels"
    mc:Ignorable="d">

    <!--
        The ramp column is user-resizable via GridSplitter rather than a hardcoded width.
        GridSplitter, not PropertySizer: a ColumnDefinition.Width is a GridLength, and
        PropertySizer manipulates a double (its canonical target is
        NavigationView.OpenPaneLength). Both ship in CommunityToolkit.WinUI.Controls.Sizers.
    -->
    <Grid Padding="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="320"
                MinWidth="240"
                MaxWidth="480" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.ColumnSpan="3" Margin="0,0,0,16" Spacing="4">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Palette" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="Skin colors come in sets of five shades, darkest first. Recoloring swaps each shade for the matching one in the set you choose."
                TextWrapping="Wrap" />
        </StackPanel>

        <!--  Ramp list  -->
        <Grid Grid.Row="1" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ListView
                x:Name="RampList"
                AutomationProperties.AutomationId="RampList"
                AutomationProperties.Name="Skin ramps"
                ContainerContentChanging="OnRampContainerChanging"
                ItemsSource="{x:Bind ViewModel.RampView, Mode=OneWay}"
                SelectedItem="{x:Bind ViewModel.SelectedRamp, Mode=TwoWay}"
                ToolTipService.ToolTip="Every color set you can use, sorted by name. Pick one to open it in the editor on the right.">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="palettes:SkinRamp">
                        <StackPanel Padding="0,8" Spacing="6">
                            <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind Name}" />
                            <StackPanel Orientation="Horizontal" Spacing="2">
                                <Border
                                    Width="32"
                                    Height="16"
                                    Background="{x:Bind local:PalettePage.StepBrush(Steps, 0)}"
                                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource ControlCornerRadius}" />
                                <Border
                                    Width="32"
                                    Height="16"
                                    Background="{x:Bind local:PalettePage.StepBrush(Steps, 1)}"
                                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                    BorderThickness="1" />
                                <Border
                                    Width="32"
                                    Height="16"
                                    Background="{x:Bind local:PalettePage.StepBrush(Steps, 2)}"
                                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                    BorderThickness="1" />
                                <Border
                                    Width="32"
                                    Height="16"
                                    Background="{x:Bind local:PalettePage.StepBrush(Steps, 3)}"
                                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                    BorderThickness="1" />
                                <Border
                                    Width="32"
                                    Height="16"
                                    Background="{x:Bind local:PalettePage.StepBrush(Steps, 4)}"
                                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource ControlCornerRadius}" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <CommandBar
                Grid.Row="1"
                DefaultLabelPosition="Right"
                HorizontalAlignment="Left">
                <AppBarButton
                    AutomationProperties.AutomationId="BtnNewRamp"
                    Command="{x:Bind ViewModel.NewRampCommand}"
                    Icon="Add"
                    Label="New"
                    ToolTipService.ToolTip="Adds a color set you can edit. It starts as a copy of the default skin tones." />
                <AppBarButton
                    AutomationProperties.AutomationId="BtnDuplicateRamp"
                    Command="{x:Bind ViewModel.DuplicateRampCommand}"
                    Icon="Copy"
                    Label="Duplicate"
                    ToolTipService.ToolTip="Copies the selected set into an editable one. This is how you change a set that came with the app." />
                <AppBarButton
                    AutomationProperties.AutomationId="BtnDeleteRamp"
                    Command="{x:Bind ViewModel.DeleteRampCommand}"
                    Icon="Delete"
                    Label="Delete"
                    ToolTipService.ToolTip="Removes the selected set. The seven sets that came with the app cannot be removed." />
                <CommandBar.SecondaryCommands>
                    <AppBarButton
                        AutomationProperties.AutomationId="BtnImportRamps"
                        Command="{x:Bind ViewModel.ImportCommand}"
                        Label="Import CSV"
                        ToolTipService.ToolTip="Reads color sets out of a .csv file and adds them to your list." />
                    <AppBarButton
                        AutomationProperties.AutomationId="BtnExportRamps"
                        Command="{x:Bind ViewModel.ExportCommand}"
                        Label="Export CSV"
                        ToolTipService.ToolTip="Writes your color sets to a .csv file you can back up or send to someone else." />
                </CommandBar.SecondaryCommands>
            </CommandBar>
        </Grid>

        <!--
            Canonical form from the toolkit sample: an Auto-width column between the two content
            columns, with a TranslateTransform pulling the splitter back over the boundary. Without
            the transform a 16px splitter sits beside the seam rather than on it.
        -->
        <controls:GridSplitter
            Grid.Row="1"
            Grid.Column="1"
            Width="16"
            AutomationProperties.AutomationId="RampColumnSplitter"
            AutomationProperties.Name="Resize ramp list"
            ResizeBehavior="BasedOnAlignment"
            ResizeDirection="Auto">
            <controls:GridSplitter.RenderTransform>
                <TranslateTransform X="-7" />
            </controls:GridSplitter.RenderTransform>
        </controls:GridSplitter>

        <!--  Editor  -->
        <ScrollViewer Grid.Row="1" Grid.Column="2" Padding="16,0,0,0">
            <StackPanel Spacing="16">

                <InfoBar
                    AutomationProperties.AutomationId="BuiltInRampInfoBar"
                    IsClosable="False"
                    IsOpen="{x:Bind ViewModel.IsBuiltInSelected, Mode=OneWay}"
                    Message="This color set came with the app, so it cannot be changed. Click Duplicate to get a copy you can edit."
                    Severity="Informational" />

                <!--
                    One InfoBar, driven by StackedNotificationsBehavior. It queues messages and
                    dismisses each after its Duration, so a run that produces several does not
                    clobber them down to one string. The behavior is the reason the view model
                    has no StatusMessage/HasStatus pair.
                -->
                <InfoBar AutomationProperties.AutomationId="PaletteStatusBar">
                    <interactivity:Interaction.Behaviors>
                        <behaviors:StackedNotificationsBehavior x:Name="StatusNotifications" />
                    </interactivity:Interaction.Behaviors>
                </InfoBar>

                <StackPanel Spacing="4">
                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Name" />
                    <TextBox
                        AutomationProperties.AutomationId="RampName"
                        AutomationProperties.Name="Ramp name"
                        IsEnabled="{x:Bind ViewModel.IsEditable, Mode=OneWay}"
                        Text="{x:Bind ViewModel.EditedName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        ToolTipService.ToolTip="What this set is called in the list here and in the skin tone list on the Pipeline page. Two sets cannot share a name." />
                </StackPanel>

                <StackPanel Spacing="8">
                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Steps" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="Five shades, darkest at the top and lightest at the bottom. Keep them in that order or the recolored art will look wrong."
                        TextWrapping="Wrap" />
                    <ItemsControl IsEnabled="{x:Bind ViewModel.IsEditable, Mode=OneWay}" ItemsSource="{x:Bind ViewModel.Steps, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:RampStepViewModel">
                                <Grid
                                    Margin="0,0,0,8"
                                    ColumnSpacing="12"
                                    VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="72" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="140" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock VerticalAlignment="Center" Text="{x:Bind Label}" />

                                    <!--
                                        ColorPickerButton is the button-plus-flyout wrapper, so
                                        there is no Flyout, no Tag-carried index and no
                                        ColorChanged handler here. SelectedColor two-way binds
                                        straight to the step.
                                    -->
                                    <controls:ColorPickerButton
                                        Grid.Column="1"
                                        AutomationProperties.AutomationId="{x:Bind SwatchAutomationId}"
                                        AutomationProperties.Name="{x:Bind Label}"
                                        SelectedColor="{x:Bind PickerColor, Mode=TwoWay}"
                                        ToolTipService.ToolTip="Click to choose this shade. The preview below follows along right away, before you save.">
                                        <controls:ColorPickerButton.ColorPickerStyle>
                                            <Style TargetType="controls:ColorPicker">
                                                <Setter Property="IsAlphaEnabled" Value="False" />
                                                <Setter Property="IsColorPaletteVisible" Value="True" />
                                            </Style>
                                        </controls:ColorPickerButton.ColorPickerStyle>
                                    </controls:ColorPickerButton>

                                    <TextBox
                                        Grid.Column="2"
                                        AutomationProperties.AutomationId="{x:Bind HexAutomationId}"
                                        AutomationProperties.Name="{x:Bind HexFieldName}"
                                        Text="{x:Bind Hex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        ToolTipService.ToolTip="The same shade written as a hex code, like #DBA463. Type over it to set an exact color.">
                                        <interactivity:Interaction.Behaviors>
                                            <!--  Retyping a hex should not need a manual select-all first.  -->
                                            <behaviors:AutoSelectBehavior />
                                        </interactivity:Interaction.Behaviors>
                                    </TextBox>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Button
                        AutomationProperties.AutomationId="BtnSaveRamps"
                        Command="{x:Bind ViewModel.SaveRampCommand}"
                        Content="Save ramp"
                        Style="{StaticResource AccentButtonStyle}"
                        ToolTipService.ToolTip="Saves the name and all five shades. Your edits are not kept until you click this." />
                </StackPanel>

                <StackPanel Spacing="8">
                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Preview" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="A sample character recolored with the shades above, so you can see the set before you save it."
                        TextWrapping="Wrap" />
                    <Border
                        Padding="16"
                        Background="{ThemeResource CanvasBackdropBrush}"
                        BorderBrush="{ThemeResource ToolPanelBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource OverlayCornerRadius}">
                        <Grid>
                            <Image
                                x:Name="PreviewImage"
                                AutomationProperties.AutomationId="RampPreviewImage"
                                AutomationProperties.Name="Recoloured sprite preview"
                                HorizontalAlignment="Center"
                                Stretch="None" />
                            <TextBlock
                                x:Name="PreviewHint"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="Set your three pack folders in Settings to see a live preview here." />
                        </Grid>
                    </Border>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
